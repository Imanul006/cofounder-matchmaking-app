import 'package:flutter/material.dart';

class Shadows {
  static const BoxShadow cardShadow = BoxShadow(
      color: Color.fromARGB(20, 112, 144, 176),
      offset: Offset(0, 16),
      blurRadius: 40);
}
